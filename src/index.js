import React from 'react';

import ReactDOM from 'react-dom';
import FirstComponent from './rootcomponent/root';

var location = document.getElementById("root")

ReactDOM.render(<FirstComponent></FirstComponent>, location);
